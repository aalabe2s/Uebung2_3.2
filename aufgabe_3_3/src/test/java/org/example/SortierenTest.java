package org.example;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SortierenTest {

    String [][] sequence = {{"A", "B"},
                            {"C", "D"},
                            {"E", "F"}};
    String [] array= {"A", "B", "D", "C"};
    Sortieren s = new Sortieren();
    @Test
    void test(){
        String [] s2 = s.isWellSorted(array);

        while (i<array.length){
            for(j = 0; j<sequence[0].length; j++){
                for(k=0; k<sequence.length)
            }
            if (array[i] == sequence)
        }

        for (i = 0; i < array.length; i++){
            for (j = 0; j<sequence.length; j++){

            }
        }


    }

}